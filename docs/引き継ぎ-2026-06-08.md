# セッション引き継ぎメモ — 2026-06-08

次回セッション開始時に読むこと。

---

## このセッションで行ったこと

### SUPABASE_SERVICE_KEY の設定

- `.claude/settings.local.json`（gitignore 済み）に service_role キーを `env.SUPABASE_SERVICE_KEY` として保存
- 目的: Claude が `feedbacks` テーブルを直接読み取り、フィードバックに対応する運用
- **現状の障壁**: Claude Code on the web 環境はアウトバウンドネットワークが制限されており、`supabase.co` へのリクエストが「Host not in allowlist」でブロックされる
- **解決策（どちらか）**:
  1. 環境の Network policy に `uqofdnsolmrzxckmtpzv.supabase.co` を追加（web 環境からも使えるようになる）
  2. ローカルの Claude Code CLI を使う（ネットワーク制限なし）

---

## アイデア台帳に追記した内容（実装はまだ）

`docs/アイデア台帳.md` の 🟠 中 セクションに追加：

- **案件のコピーから新規案件を作成する機能**
  - クラウド案件一覧から「コピーして新規作成」→ 全データをコピーした新規案件を開く
  - `gatherAllData()` / `_applyQuoteData()` の既存フローを流用
  - `quote_presets` から読み込んで `id` を null にして再保存

---

## 現在のブランチ状態

- 開発ブランチ: `claude/repository-idea-storage-O04WN`
- このセッションでのコード変更: なし（アイデア台帳・引き継ぎメモの更新のみ）

---

## 次回セッションで着手できそうなこと

優先度・依存関係から見た実装順：

```
サーチャージマスター（マスタータブ新設）★最優先
    ↓
顧客マスター
    ↓
料金改定通知管理（②③④）
    ↓
案件コピー機能（単体で実装可・依存なし）
    ↓
案件チャット
    ↓
共同編集（Presenceフィールドロック）※データ構造変更が先決
```

フィードバック読み取り運用は、Supabase へのネットワークアクセスが解決したタイミングで自然に開始できる。
