# アイデア台帳 — フォワーダー支援

最終更新: 2026-06-08

改修要望・機能アイデア・UX改善案を優先度別に管理するファイル。
実装が完了した項目は `[x]` に変え、対応コミットを添える。

## 優先度の目安

| マーク | 意味 |
|--------|------|
| 🔴 高 | 業務で毎日困っている・客先への影響あり |
| 🟠 中 | あると便利・クオリティ向上 |
| 🟡 低 | 将来的にほしい・アイデア段階 |
| 🔵 検討 | 方針決定が必要・要調査 |

---

## 🔴 高：業務直結・早急に対応したいもの

- [ ] **消費税表示の見直し（課税チェック済み行のみ課税として扱う）**
  - 背景: 現状の課税チェックボックスの動作を整理し、チェックが入っている行のみを課税扱いとするシンプルな仕様に統一したい
  - 解決策: カテゴリ別のデフォルトフラグ設定よりも、ユーザーが明示的にチェックした行のみを課税とする現状の仕組みを維持しつつ、プレビュー上で「課税X件・非課税Y件・消費税計¥Z」のサマリを表示して確認しやすくする
  - 注意: 輸出免税の自動判定はこのアプリの現状スコープ外のため対応しない
  - 影響ファイル: `preview.js`（サマリ表示追加のみ）

---

## 🟠 中：あると便利・品質向上

- [ ] **Excel / CSV との双方向連携**
  - 背景: サーチャージ案内・顧客リスト等のデータは Excel で管理・配布されることが多く、アプリへの取り込みや逆方向の出力ニーズがある
  - 現状: 見積明細の Excel 出力は実装済み（SheetJS）。インポート機能は未実装
  - 設計方針:
    - **短期 — テンプレート方式**: サーチャージマスター・顧客マスター実装時にダウンロード用テンプレート Excel と読み込み機能をセットで作る。列が少ない初期段階はこれで十分
    - **中期 — 列マッピング方式**: 任意の Excel/CSV をアップロード → 列ヘッダーをアプリのフィールドにドロップダウンで対応付け → プレビュー確認 → 一括インポート。船会社ごとに微妙に異なる書式にも対応可能（Notion/Airtable と同じ考え方）
  - 想定する連携場面:
    - サーチャージ一括登録（船会社から届く Excel 添付をそのまま取り込む）
    - 顧客マスター一括登録
    - 案件一覧のエクスポート（案件管理を Excel で並行管理したい場合）
  - 注意点: 列順・列名のズレによる読み込みエラー対策・バリデーションを厚く作ることが重要
  - 前提: サーチャージマスター・顧客マスターの実装後に着手
  - 優先度: マスター実装と同タイミングで短期版を作るのが自然

- [ ] **見積有効期限の警告・失効ロック**
  - 背景: 期限切れ案件を気づかずに使い回す、または期限管理のタイミングを逃すケースがある（初級・中級・上級 全員が指摘）
  - 解決策:
    - 有効期限フィールドが過去日付の場合、見積タブ上部を赤枠ハイライト
    - クラウド一覧カードに「期限切れ」「残〇日」バッジを色付き表示
    - 発行日横に「+7日」「+30日」ワンクリックボタンを追加
    - クラウドプレビューを開いたとき期限超過なら「再見積として複製しますか？」を提案
  - 影響ファイル: `cloud.js`（一覧カード）、`conditions.js`（有効期限フィールド）

- [ ] **為替レートのスナップショット保存と書面への記載**
  - 背景: プリセット保存後に為替が更新されると再表示時に金額がズレ、客先提示額との差異が生じる。また取得日時が書面に出ないため後追いできない（上級者指摘）
  - 解決策: 保存時に使用レート・取得タイムスタンプをデータに封じ込める。プレビュー・PDFに「1 USD = ¥152.30（2026-06-09 09:15 取得）」を自動付記。保存済み案件を開いた際は保存時レートを使用し、最新レートへの更新は手動で選択可能にする
  - 影響ファイル: `save.js`、`preview.js`、`shared/fx.js`
  - 優先度メモ: 監査・社内精算に直結（上級者最優先指摘）

- [ ] **通貨別小計サマリの常時表示**
  - 背景: USD・JPY・EUR 混在案件で通貨別の内訳を確認するためにプレビューを開く必要があり手間（初級・中級 双方指摘）
  - 解決策: 見積テーブルのツールバー直下に「USD: 1,200.00 / JPY: ¥85,000 → 円換算合計: ¥217,000」のサマリバーを常時表示。既存の `updateTotals()` を拡張するだけで実装可能

- [ ] **インコタームズ選択時の費用負担範囲インライン表示**
  - 背景: DAP・CIP・CIF など選ぶたびに「知識・参考」タブを行き来して確認している（初級者指摘）
  - 解決策: インコタームズ選択プルダウンで選んだ瞬間、「売主負担 / 買主負担の費用範囲サマリ」をインラインまたはツールチップで表示。既存の知識タブの内容を流用可能

- [ ] **顧客名オートコンプリート（クラウド案件から暫定）**
  - 背景: 同じ顧客に繰り返し見積を出しているのに会社名・担当者名を毎回手入力しており、スペルミスや表記ゆれが起きやすい（初級者指摘）
  - 解決策: `qf-customer` 入力時にクラウド保存済み案件から顧客名を集計してオートコンプリート候補表示。顧客マスター実装前の暫定対応として有効。選択時に `qf-person` も過去値から自動補完

- [ ] **類似案件サジェスト（条件一致で過去案件を候補表示）**
  - 背景: 同じルート・インコタームズの過去見積を探して費用構成を参考にしたいが、クラウド一覧で毎回検索する手間がかかる（初級者指摘）
  - 解決策: POL・POD・輸送モード・インコタームズが入力されたタイミングで、一致条件の過去案件を最大3件サジェストするパネルを条件欄下部に表示

- [ ] **プレビュー前チェックにサブコン未入力行の警告を追加**
  - 背景: 利益計算を意図した行のサブコン欄が空のまま出力してしまうケースがある。現状の警告は単価ゼロ・項目名空白のみ（中級者指摘）
  - 解決策: `preview.js` の `_applyPreviewHighlights` に「費用カテゴリが domestic / customs / overseas なのにサブコン欄が空」の行を警告対象として追加
  - 影響ファイル: `preview.js`（数行の追加で対応可能）

- [ ] **寸法入力の単位切り替え（mm / cm）**
  - 背景: 荷主からのパッキングリストが mm 単位の場合、cm 固定の入力欄に換算しながら打ち直す作業が発生する（中級者指摘）
  - 解決策: 荷姿明細テーブルの寸法入力欄に cm / mm のトグルを追加。mm 選択時は入力値を ÷10 して CBM 計算に反映
  - 影響ファイル: `cargo.js`（`calcLiveUpdate()` に単位係数を追加）

- [ ] **LCL最低運賃（Min）ガードとR/T計算根拠の明示**
  - 背景: 最低運賃の入力欄がなく、1RT 未満でも最低運賃が発生するのに計算上0円になる。また R/T 行で CBM / 重量どちらを採用したかが見積書面に表示されない（上級者指摘）
  - 解決策: 単位「R/T」行の備考または注記に「CBM: X / 重量換算: Y → 高い方を適用」を自動挿入。「最低運賃（Min）」行の追加を促す注意テキストを表示

- [ ] **リマーク欄・社内メモ行の出力対象を視覚的に明示**
  - 背景: リマーク欄の内容が PDF・メールに含まれるかどうかが直感的に分かりにくく、社内用コメントと顧客向け注記を混在させてしまうリスクがある（初級者指摘）
  - 解決策: リマークテキストエリアの上部に「この内容は PDF・メール出力に含まれます」を常時表示。社内メモ行（🔒）との違いも合わせて一言補足

- [ ] **案件のコピーから新規案件を作成する機能**
  - 背景: 類似した条件の案件を繰り返し作成するケースが多く、毎回ゼロから入力するのは非効率
  - 期待する動作:
    - クラウド案件一覧（またはプレビューモーダル）から「コピーして新規作成」ボタンを押すと、その案件の全データ（条件・明細・備考等）をコピーした新規案件が開く
    - コピー元の案件名に「(コピー)」等のサフィックスを付けて区別
    - コピー後はクラウド未保存の状態で編集可能（保存ボタンで別案件として登録）
  - 技術メモ:
    - `gatherAllData()` でデータ取得 → `_applyQuoteData()` で適用、という既存フローを流用できる
    - クラウド保存済み案件のコピーは `quote_presets` からデータを読み込んで `id` を null にして再保存
  - 前提: クラウドプリセット保存機能（実装済み）
  - UI候補: クラウドプレビューモーダルのヘッダー or アクションボタン列に追加

---

## 🟡 低：将来アイデア・検討中

- [ ] **見積タブの3ページ構成リデザイン**
  - 背景: 現状は条件・明細・統計が1画面に詰まっており、特にスプリットビューで窮屈
  - 構成案（合意済み）:
    - **1ページ目（ダッシュボード）**: 簡易統計（粗利率・合計金額推移など）、進捗管理（受注/失注/保留）、見積検索・一覧、新規見積作成ボタン
    - **2ページ目（条件入力）**: 輸送モード・引き合い条件・貨物・物量情報・マイルストーン
    - **3ページ目（明細入力）**: 現在の見積テーブル（費用明細・プレビュー・出力）
  - メリット:
    - 各ページが1つの役割に集中できる
    - 1ページ目は Supabase 連携後に統計・検索が自然に載る器になる
    - 2→3ページへの遷移で「条件確定 → 明細入力」のフローが明確になる
  - 前提: Supabase 基盤（統計・検索に必要）が整うタイミングで設計・実装

- [ ] **輸出+輸入セット対応（方向・マイルストーン）**
  - 背景: 輸出→輸入・輸入→輸出をワンセットで扱う案件が多数ある
  - 検証結果（合意済み）:
    - 輸送モード（FCL/LCL/Air）自体は方向に依存しないため変更不要
    - 費用カテゴリも既に輸出・輸入の両方が選択可能
    - **対応が必要な箇所は3箇所のみ**:
      - `index.html`: 方向ボタンに「↔️ 輸出+輸入」を追加
      - `conditions.js`: `_applyZoneLabels()` の 'both' 対応
      - `quote-email.js`: `dirMap` に `both: '輸出+輸入'` を追加
    - **マイルストーンは A案（2段表示）を推奨**: 輸出バーと輸入バーを縦に並べる。`renderQuoteMilestones` を export / import で2回呼ぶ設計。6ゾーン1本より業務実態に合う。
  - 変更量: 方向ボタン対応は小、マイルストーン2段表示は中

- [ ] **Supabase連携：行挿入パネル（行パターン＋サブコン別モジュールの統合UI）**
  - 背景:
    - 既存の「行パターン登録」はlocalStorage保存でチーム共有できない
    - サブコン別モジュール挿入（アイデア）と統合することで1つの「＋ 挿入」パネルに集約できる
  - 設計方針（合意済み）:
    - **2つは競合しない。同じパネルにタブ分けして共存させる**
    - 「保存パターン」タブ: 手動で名前をつけて登録した定型セット（現在の行パターン機能をSupabaseに移行）
    - 「サブコン別」タブ: 案件蓄積から自動生成。保存ステップ不要・直近案件が自動的に最新版になる
    - 両タブとも Supabase に保存 → チーム全員で共有・閲覧・挿入できる
  - 使い分け:
    - 保存パターン → 「社内標準セット」「航空基本セット」など意図的に管理したいテンプレ
    - サブコン別 → 「先月 ABC Trucking で使った費用をベースにしたい」など実績単価の流用
  - UIイメージ:
    ```
    「＋ 挿入」パネル
    [保存パターン] [サブコン別]
    ─────────────────────────
    保存パターンタブ:
      標準輸出セット（8行）
      航空基本セット（5行）
    サブコン別タブ:
      ABC Trucking（最終: 2026-06-01, 5行）
      横浜税関代行（最終: 2026-05-30, 3行）
    ```
  - 前提: Supabase への見積保存機能・認証基盤が先に必要

---

## 🔵 検討：方針・仕様確認が必要なもの

- [ ] **Supabase連携：案件蓄積によるマスター自動生成＋受注/失注分析**
  - 背景: 案件を貯めるだけで顧客・サブコン・費用項目の「マスター」が自然と出来上がる設計にしたい
  - 設計方針（合意済み）:
    - マスターテーブルは別途作らない。案件データへの集計クエリがマスター代わり
    - 顧客マスター: `quotes` の customer_name を DISTINCT 集計
    - サブコンマスター: `quote_rows` の subcon_name を DISTINCT 集計
    - 費用項目マスター: `quote_rows` の name を DISTINCT 集計＋平均単価・使用回数付き
    - 案件が増えるほど精度が上がる（50案件で顧客20社・費用項目80件相当のマスターが生える）
  - 活用例:
    - 品目入力時のオートコンプリート＋前回単価の自動補完
    - モジュール挿入のサブコン検索元（別アイデアと連携）
    - 顧客別・サブコン別のコスト傾向分析
  - 残課題: 表記ゆらぎ（例: "通関料" vs "通関費用"）は別途名寄せ作業が必要
  - 将来拡張: 使用頻度の高い項目を「正式マスター」に昇格させる仕組みを後付け可能

- [ ] **Supabase連携：受注/失注率の分析**
  - 背景: 見積の結果（受注・失注・保留）を記録することで勝率・傾向を把握したい
  - 想定する分析軸:
    - 顧客別・担当者別・ルート別・輸送モード別の受注率
    - 粗利率帯別の受注率（安い見積が取れているか）
    - 失注理由の傾向（価格・リードタイム・サービス等）
    - 月次・四半期の受注金額推移
  - 必要な追加フィールド: `status`（受注/失注/保留/提出済み）, `lost_reason`, `won_at`
  - 前提: Supabase への見積保存機能が先に必要


- [ ] **AIエージェントによるブッキング自動化**
  - 背景: フォームでブッキング依頼を受け付け、AIエージェントが各船会社ポータルでブッキングを完了させる
  - 段階的な実装方針（合意済み）:
    - **Stage 1**（実現しやすい）: AI が依頼内容を整形 → 各社フォームの入力値を自動生成 → 人が確認してワンクリック送信。現状の SI 作成の延長。入力作業の9割を省ける
    - **Stage 2**（中難度）: 公式 API 対応の船会社（Maersk・CMA CGM 等）のみ完全自動化
    - **Stage 3**（高難度・将来）: Claude Computer Use + Playwright で残りの船会社ポータルを自動操作
  - 現実的な壁:
    - 17社分の個別実装が必要（構造がすべて異なる）
    - CAPTCHA・2FA・アンチbot対策
    - ポータルUI変更による保守コスト
    - 各社利用規約の確認が必要（自動アクセス禁止の場合あり）
  - 前提: Supabase への依頼データ保存・お気に入りリンク（各社URL管理）が先に必要


- [ ] **将来構想：実務メニューと見積メニューの入り口分離**
  - 背景: 役割が異なるユーザー（オペレーション vs 営業/価格担当）が同じポータルを使うなら入り口を分けた方が使いやすい
  - 設計方針（合意済み）:
    - 実務メニュー: Booking/SI・スケジュール・お気に入りリンク・書類・通関・知識参考
    - 見積メニュー: 見積作成・費用計算・プレビュー/出力・受注/失注管理
    - データ層（Supabase）は共有: 顧客・サブコン・費用項目・お気に入りリンク・為替レート
  - メリット:
    - 実務側は軽快に・モバイル対応優先
    - 見積側は情報量を確保
    - 両側で同じマスターデータを参照できる
  - 前提: Supabase基盤の整備・マスター自動生成・お気に入り共有が揃ってから

- [ ] **お気に入り共有機能（船会社メニューのブックマーク）**
  - 背景: 船会社ごとに異なるポータルのレイアウトから目的のメニューを毎回探すのが非効率
  - 設計方針（合意済み）:
    - Supabaseに船会社別リンクを保存（carrier_name / label / category / url / note）
    - アプリ内で船会社・カテゴリでフィルターして一覧表示
    - チームメンバー全員が追加・編集できる（Supabase RLS で制御）
  - UIイメージ: 船会社タブ＋カテゴリフィルター＋リンク一覧＋追加ボタン
  - 配置候補: 既存「🚢本船・航路」タブへの組み込み or 新タブ
  - 前提: Supabase認証基盤が必要

- [ ] **顧客マスターの新設**
  - 背景: 現在の見積フォームには「会社名」「担当者名」しかなく、メールアドレス・部署・電話番号等が管理できていない。サーチャージ通知の宛先管理や見積フォームとの連動を考えると情報が不足している
  - 管理項目（確定）:
    - `customer_code`: アプリ内独自の管理番号（例: `C-001`）。社内基幹システムにすでにコードが存在する場合はそちらに合わせる形も可
    - `company_name`: 会社名（既存 `qf-customer` と連動）
    - `department`: 部署名（宛名出力に使用）
    - `person_name`: 担当者名（既存 `qf-person` と連動）
    - `email`: 担当者メールアドレス（通知宛先の自動セット）
    - `phone`: 電話番号
    - `note`: 備考（自由記述）
    - `created_by` / `updated_at`
  - 意図的に除外した項目:
    - 輸送モード・インコタームズ・取引通貨・支払条件 → 当面不要
    - 通知フラグ → 将来の「定期案内」機能側で管理する（顧客マスターには持たせない）
  - 顧客コードの方針:
    - 基本はアプリ内で独自採番（基幹システムへの依存を避ける）
    - 既存の基幹システムコードがある場合は `customer_code` にそれを使う形でも運用可
    - 基幹システムとの自動連携（API同期等）は将来拡張として検討
  - 見積フォームとの連動（将来）:
    - `qf-customer` 入力時に顧客マスターをサジェスト → 選択すると担当者・部署・メールが自動セット
    - 見積保存時に `customer_code` も一緒に記録し、案件←→顧客の紐付けを確立
  - テーブル案: `customers { id, customer_code(unique), company_name, department, person_name, email, phone, note, created_by, updated_at }`
  - RLS: `is_team_member()` で読み書き制御
  - 前提: Supabase 認証基盤（実装済み）
  - 依存: 「料金改定・サーチャージ通知管理」の宛先グループ機能と連動

- [ ] **料金改定・サーチャージ通知の管理・簡素化**
  - 背景: 客先への料金改定やサーチャージ導入の案内を定期的に行っているが、宛先の選定・文面作成・案内履歴の管理がすべて手作業で工数がかかる。抜け漏れや二重案内も発生しやすい
  - 実装したい機能（優先度順）:
    - **② 顧客グループ別の宛先管理**
      - 顧客マスターに輸送モード属性（FCL / LCL / AIR、複数可）を持たせる
      - 通知作成時に「FCL向け」「LCL向け」等を選ぶと該当顧客を自動抽出・一覧表示
      - 通知対象から除外したい顧客をチェックで外せる（個別調整）
    - **③ 通知履歴の記録**
      - 誰に・いつ・何の改定を・誰が案内したかを記録
      - テーブル案: `surcharge_notifications { id, surcharge_ids[], customer_ids[], notified_at, notified_by, note }`
      - 顧客一覧で「最終案内日」「案内済みサーチャージ」をひと目で確認できる
      - 「この顧客にはまだ案内していない」の抽出フィルターも対応
    - **④ 通知テンプレート管理**
      - 「GRI案内」「PSS導入」「料金改定のお知らせ」等の定型テンプレートを登録・管理
      - テーブル案: `notification_templates { id, name, subject, body_template, created_by }`
      - テンプレート本文にプレースホルダ（`{{carrier}}` `{{charge_name}}` `{{amount}}` `{{valid_from}}` 等）を埋め込み、サーチャージマスターの内容を差し込んで文面生成
      - 生成した文面はクリップボードコピー or メールクライアント起動（`mailto:`）で利用
  - UX の流れ（想定）:
    1. サーチャージマスターで「今回案内する改定」にチェック
    2. 通知テンプレートを選択
    3. 宛先グループ（FCL/LCL等）を選択 → 顧客一覧が自動抽出・確認
    4. 文面プレビューを確認してコピー or 送信
    5. 案内完了後に通知履歴として自動記録
  - 前提: 顧客マスター・サーチャージマスター・Supabase 認証基盤
  - 依存: 上記「マスタータブ新設」「サーチャージマスター」の実装後

- [ ] **マスタータブの新設（繰り返し登場する要素の一元管理）**
  - 背景: 案件が蓄積されると顧客・費用項目・サブコン・サーチャージなど繰り返し登場する要素が増える。現状はメール・社内サーバー・個人PC等に散在しており管理方法が統一されていない
  - **最優先: サーチャージマスター**
    - 各船会社・LCLキャリアーからのサーチャージアナウンスを一元管理
    - 登録項目（案）: `carrier`（船会社/キャリア名）/ `charge_name`（サーチャージ名）/ `amount`（金額）/ `currency`（通貨）/ `unit`（単位: per W/M, per B/L 等）/ `valid_from`〜`valid_to`（適用期間）/ `transport_mode`（FCL/LCL/AIR）/ `note`（備考・アナウンス原文リンク等）/ `source`（情報ソース: メール/公式サイト等）/ `created_by` / `updated_at`
    - 機能:
      - 一覧表示（会社別・期間別フィルター）
      - 新規登録・編集・削除（チームメンバー全員可）
      - 有効期限切れのアナウンスをグレーアウト or アーカイブ
      - 将来: 見積作成時にサーチャージマスターから行を自動提案・挿入
    - 現状課題: メール添付・社内サーバー保管・個人管理が混在しており参照先が定まっていない
  - **将来拡張（マスタータブに追加予定の要素）**:
    - 顧客マスター（顧客名・担当者・住所・インコタームズ傾向等）
    - サブコン単価マスター（よく使うサブコンの標準費用）
    - 費用項目マスター（案件蓄積からの自動生成 → 既存アイデア「Supabase連携：案件蓄積によるマスター自動生成」と統合）
  - UI配置: ポータルのタブバーに「🗂 マスター」タブを新設。サーチャージ・顧客・サブコン等をサブタブで切り替え
  - 技術検討事項:
    - テーブル: `surcharge_master { id, carrier, charge_name, amount, currency, unit, valid_from, valid_to, transport_mode, note, source, created_by, updated_at }`
    - RLS: `is_team_member()` で読み書き制御
    - 有効期限管理: `valid_to < now()` で自動的に期限切れ判定
  - 前提: Supabase 認証基盤（実装済み）
  - 優先度メモ: サーチャージマスターは他のマスターより実務インパクトが高いため先行実装を推奨

- [ ] **AMS / ISF・AFR・ENS 申告要否の自動フラグ表示**
  - 背景: POD（仕向港）と輸送モードが確定した段階で AMS/ISF・ENS 等の申告要否を判断する必要があるが、判断ミスや行の入れ忘れが発生しやすい（中級者指摘）
  - 解決策: POD の国コードと輸送モードから「USA向け海上→AMS/ISF必須」「EU向け→ENS必須」等のルールをテーブル定義し、条件確定時に警告バッジまたはワンクリック行挿入ボタンを表示。`constants.js` の `SCOPE_PRESETS` に既に AMS/ISF の行定義があるため表示ロジックの追加で実装可能
  - 影響ファイル: `conditions.js`、`constants.js`

- [ ] **複数案件の並列比較ビュー**
  - 背景: 同一顧客・同一ルートで複数キャリアの見積を作成した場合、差分をひと目で比較する手段がない（中級者指摘）
  - 解決策: クラウド一覧でチェックボックス複数選択 → 「比較」ボタンで合計・粗利率・条件の差分を並べたモーダル表示。フル画面リデザインではなく軽量な比較モーダルとして実装

- [ ] **HSコード・輸出規制スクリーニング補助**
  - 背景: `cond-hs` フィールドがフリーテキストのため誤記や外為法スクリーニングの見落としが起きやすい（上級者指摘）
  - 解決策: HSコード上4桁入力時にリスト規制の主要類ヘッダに該当するかを警告バッジで表示。完全な該非判定ではなく「経産省リスト規制対象の可能性があります。確認を忘れずに」という注意喚起として実装。新人教育の観点でも有効
  - 注意: 法的判断には使えない旨の免責表示が必要

- [ ] **危険品・温度管理貨物の詳細入力パネル**
  - 背景: 現状の危険品フィールドはフリーテキストのため IMDG Class・UN番号・PG・FlashPoint・技術名称が記録されず、ブッキング時に船会社から差し戻されるケースがある（上級者指摘）
  - 解決策: 危険品トグルを ON にすると IMDG Class（1〜9）・UN番号・PG・Flash Point・技術的正式品名の入力パネルを展開。未入力のままプレビュー出力しようとすると確認ダイアログを表示。PDF・メールへも自動記載

- [ ] **見積根拠ログ・変更履歴（監査トレーサビリティ）**
  - 背景: 現状は `updated_at` と `owner_email` しか残らず、金額変更の理由・変更前後の数値を後追いできない。内部監査・顧客クレーム対応で根拠を求められた際に詰む（上級者指摘）
  - 解決策: クラウド保存のたびに `quote_preset_logs { preset_id, snapshot(jsonb), changed_by, changed_at, change_note }` に差分スナップショットを積む。プレビューモーダルに「変更履歴」タブを追加し、保存時に任意の変更メモを1行入力可能にする
  - テーブル案: `quote_preset_logs { id, preset_id, snapshot jsonb, changed_by, changed_at, change_note text }`

- [ ] **案件の共同編集対応（Presence + フィールドロック）**
  - 背景: 複数人が同じ見積を同時に開いて編集するケースがある。現状は後勝ち（最後に保存した人の内容で上書き）になりデータ消失リスクがある
  - 段階的な実装方針:
    - **フェーズ1 — 保存競合の検知・警告**（前提・最小限）:
      - 保存時に `updated_at` を比較し、自分が最後に読み込んだ時刻より新しければ「他のメンバーが更新しました。上書きしますか？」と警告
      - Supabase Realtime で `quote_presets` の変更をサブスクライブ → 他者の保存をリアルタイム通知
    - **フェーズ2 — Presence によるフィールドロック**（本命）:
      - Supabase Realtime の **Presence** チャンネルで「誰が今どの案件を開いているか・どの欄にフォーカスしているか」をブロードキャスト
      - 他メンバーが編集中のフィールドは入力不可（またはハイライト警告）にしてロック
      - 編集中ユーザー名をフィールド上にツールチップ表示（例: 「🟢 田中さんが編集中」）
  - 技術検討事項:
    - **データ構造の変更が必要**: 現状は quote 全体を1つの `data jsonb` ブロブで保存している。フィールドロックを実装するにはフィールド単位（または行単位）での保存に設計変更が必要
    - **Presence チャンネル**: `supabase.channel('quote:{preset_id}').track({ user, field })` で実装可能
    - **ロックの粒度**: 明細行単位 or フィールド単位（細かいほど競合が減るが実装が複雑）
    - **切断時のロック解放**: ブラウザを閉じた場合の自動解放をどう保証するか（Presence は接続切断で自動解放されるため原理上は問題なし）
  - 前提: Supabase 認証基盤・クラウドプリセット保存（実装済み）、フェーズ1 完了後にフェーズ2 へ
  - 依存: データ構造の見直し（`data jsonb` 一括保存 → フィールド/行単位保存への移行）が先決

- [ ] **案件チャット（申し送りメモ）**
  - 背景: 複数人で1つの見積を作成する場合、途中経緯・担当者間の申し送りを残す手段がない。メール・チャットツールに散らばりがちで案件と紐付かない
  - 期待する動作:
    - クラウドプリセット（案件）単位でチャットスレッドを持つ
    - メンバーが発言を投稿・閲覧できる（投稿者名・日時付き）
    - 案件プレビューモーダル内または専用スレッドビューで表示
    - 既読管理や通知は将来拡張（まずは投稿・閲覧のみ）
  - 技術検討事項:
    - **テーブル設計**: `quote_comments { id, preset_id(→quote_presets), body, created_by, created_at }`
    - **リアルタイム更新**: Supabase Realtime（`SUBSCRIBE` channel）で他メンバーの投稿を即時反映するか、手動リロードで十分か要検討
    - **RLS**: `is_team_member()` でチームメンバーのみ読み書き可（許可メンバー全員が書込・閲覧）
    - **UI配置**: クラウドプレビューモーダル下部に折りたたみ式チャット欄が自然か
  - 前提: Supabase 認証基盤・クラウドプリセット保存機能（実装済み）が必要
  - 依存: `quote_presets` テーブルの `id` を外部キーとして使用

- [ ] **ユーザーロール管理（admin / member / viewer）**
  - 背景: メンバーが増えてきたため、管理者と一般利用者でできることを分けたい
  - 設計方針:
    - `allowed_emails` テーブルに `role` 列（text, DEFAULT `'member'`）を追加するだけで対応可能
    - ロール定義:
      - `admin`: ユーザー管理・全データ削除・設定変更
      - `member`: 見積・マスター・ブックマークの読み書き（通常業務）
      - `viewer`: 閲覧のみ（将来拡張用）
  - 実装方法:
    - Supabase に `get_my_role()` 関数を追加し RLS ポリシーで使い分け（例: 削除は admin のみ）
    - ログイン後にロールを取得して `window._myRole` に保持
    - `admin` のみユーザー管理タブ・削除ボタンを表示
  - SQL（実装時のメモ）:
    ```sql
    ALTER TABLE allowed_emails ADD COLUMN role text NOT NULL DEFAULT 'member';
    CREATE OR REPLACE FUNCTION get_my_role()
    RETURNS text AS $$
      SELECT role FROM allowed_emails WHERE email = auth.email()
    $$ LANGUAGE sql SECURITY DEFINER;
    ```
  - 移行方針: 既存メンバーは全員 `member`、オーナー（shogo.a68@gmail.com）を `admin` に設定
  - 前提: Supabase 認証基盤（実装済み）
  - 依存: アプリ内ユーザー管理 UI と合わせて実装するのが自然

- [ ] **アプリ内ユーザー管理 UI**
  - 背景: 現状は新規メンバーの追加・削除を Supabase ダッシュボードの `allowed_emails` テーブルを直接操作して行っており、管理者がダッシュボードにアクセスできる必要がある
  - 期待する動作:
    - アプリ内の管理画面（設定タブ等）からチームメンバーの一覧表示・追加・削除・ロール変更ができる
    - 追加: メールアドレスを入力して「招待」→ `allowed_emails` に INSERT
    - 削除・ロール変更: `admin` のみ操作可
  - 現在の手順（暫定）:
    1. 追加したいユーザーにまず Google アカウントでログインしてもらう
    2. Supabase ダッシュボード → Table Editor → `allowed_emails` に email を INSERT
    3. 以降そのユーザーはチームメンバーとしてデータの読み書きが可能になる
  - 前提: Supabase 認証基盤（実装済み）、ユーザーロール管理の実装後
  - 将来拡張: マルチテナント化（事業化）の際はテナントごとのユーザー管理に発展させる

- [ ] **見積テーブルの操作ガイド（目立たない形での提供）**
  - 背景: ドラッグ並び替え・数式ペースト・Ctrl+K コマンドパレット・↑↓キー移動など非自明な操作が多いが、UI 上に説明がなく発見しにくい
  - 設計方針: 目立たせすぎず、知りたい人だけが見られる形
  - UI 候補（いずれか or 組み合わせ）:
    - **① ツールバーに小さな「？」ボタン** → クリックでショートカット・操作一覧モーダルを表示。Ctrl+K の既存コマンドパレットと連携も可
    - **② キーボードショートカット `?` キー** → 見積タブがアクティブな時に `?` を押すとヘルプ表示（VS Code・GitHub スタイル）
    - **③ 初回利用時のみツールチップ** → localStorage で「初回フラグ」を管理し、初めて行追加した時だけヒントを1〜2秒表示
  - 掲載すべき操作の候補:
    - 行の並び替え（ドラッグ or ↑↓キー）
    - 単価への数式入力（例: `100*1.08` をペースト）
    - Ctrl+K コマンドパレット
    - 社内メモ行の追加
    - 列表示のトグル（列 ▼）
  - 前提: 既存の Ctrl+K コマンドパレット（実装済み）を拡張する形が自然

- [ ] **見積テーブルの単価列に計算機ボタンを追加**
  - 背景: 単価をその場で計算したい場面（為替換算・割引計算・マージン乗せ等）があるが、別途電卓を開く必要がある
  - 期待する動作: 単価セル横に小さな「🧮」ボタンを配置 → クリックでミニ計算機ポップオーバーを表示 → 計算結果を「単価に反映」ボタンで入力欄にセット
  - 備考: 現状は単価セルに `100*1.08` のような数式をペーストすると自動評価される機能が既にある（数式ペースト機能）。計算機ボタンはその機能をより発見しやすくする位置付けにもなる
  - UI候補: セルにフォーカスした時だけボタンを表示（常時表示は列幅を圧迫するため）
  - 前提: 既存の数式評価ペースト機能（実装済み）と併存

- [ ] **各モーダルへのフィードバックボタン追加**
  - 背景: 現状はページ全体に FAB が1つあるだけで、モーダル操作中にフィードバックしたい場合に画面を閉じる必要がある
  - 期待する動作: 各モーダルのヘッダーに小さな「💬」ボタンを配置 → クリックで `openFeedback(section)` を呼び出し、どのモーダルからの送信かを `section` に自動セット
  - 対象モーダル候補: クラウドプレビュー・プリセット管理・ブックマーク・FX パネル・フィードバック受信一覧など
  - 実装コスト: 低（既存の `openFeedback(section)` をそのまま流用できる）
  - 前提: フィードバック機能 Supabase 移行（実装済み）

- [ ] **アプリ内更新履歴 / お知らせ機能**
  - 背景: 機能追加・改修が頻繁に行われるため、ユーザーに対して「何が新しくなったか」を伝える手段がない
  - 設計方針: Supabase テーブルで管理し、アプリ内モーダルで表示
  - テーブル案:
    ```sql
    changelogs {
      id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
      title       text NOT NULL,       -- 例: "サーチャージマスター機能を追加"
      body        text,                -- 詳細説明（Markdown可）
      category    text,                -- 例: "新機能" / "改善" / "修正"
      released_at timestamptz NOT NULL DEFAULT now(),
      created_by  text
    }
    ```
  - RLS: 全員が SELECT 可（ログイン不要）。INSERT / UPDATE / DELETE は `admin` のみ
  - UI:
    - ポータルヘッダーに「🔔」ボタンを配置 → クリックで更新履歴モーダルを表示
    - 前回ログイン以降の新着がある場合はバッジ（件数）を表示
    - 一覧は `released_at` 降順、カテゴリ別にラベルで色分け
  - 将来拡張:
    - 未読管理（`changelog_reads { user_id, changelog_id, read_at }`）
    - 新着があれば初回ログイン時にポップアップ
  - 前提: Supabase 認証基盤（実装済み）、ユーザーロール管理（admin 判定に使用）
  - 運用: 機能リリース時に Claude がエントリを追加する運用を想定


  - 背景: アプリが実用レベルに達した場合、他社フォワーダーへの販売・譲渡・事業売却を検討したい
  - 選択肢:
    - **① SaaS として月額販売**（最も価値が高い）: コードを渡すのではなくサービスとして提供。月額数千〜数万円/社 × 契約社数 = 継続収益。自分で運営するか運営ごと売却（Exit）するか選べる
    - **② コード・事業ごと売却**: 物流テック系スタートアップ・SIer・大手フォワーダー（ヤマトHD・日本通運・近鉄エクスプレス等の新規事業部門）への譲渡
    - **③ 業界団体への提供**: JIFFA（日本国際貨物取扱業協会）等が会員向けツールとして採用。収益より業界貢献・名声という形になりやすい
  - このアプリの強み: 日本の中小フォワーダーの業務フローに特化しており、海外汎用ツール（CargoWise 等）が手を出しにくい領域
  - 今から意識しておくべきこと:
    - **技術面**: 個人の API キー・メールアドレスをコードから完全分離 / マルチテナント対応（現状は1チーム想定）/ ドキュメント整備
    - **ビジネス面**: 著作権・IP の整理（個人 or 法人で持つか）/ 利用規約・プライバシーポリシーの整備 / 競合調査（CargoWise, Logiper 等）
  - 方針メモ: 単体売却より「SaaS として育てた実績（契約社数・使用量）を持った状態で売る」方が交渉力が上がる。まずは複数チームで使える状態（マルチテナント化）を目指すのが先決

- [ ] **Supabase連携：基本設計**
  - 何を貯めるか（見積データ・プリセット・フィードバック・為替履歴）
  - 認証方法（メール認証 / Google ログイン / Magic Link）
  - チーム構成・読み書き権限の範囲
  - 現在のlocalStorage保存との移行戦略

---

## 完了済み

- [x] **フィードバック受付を Supabase に移行** ✅ 実装済み（実装時期不明）
  - `feedbacks` テーブル（category / section / body / user_id / status / created_at）に直接 INSERT
  - アプリ内モーダルの「受信一覧」タブでステータス管理（open / resolved / wontfix）
  - ログインメンバーのみ受信一覧タブを表示（`refreshFbAdminTab`）
  - ※ `index.html` のコメントに「裏で Google Form に no-cors POST」と古い記述が残存 → 要クリーンアップ
  - **Claude がフィードバックを読んで対応する運用** (2026-06-08 方針決定):
    - `SUPABASE_SERVICE_KEY`（service_role JWT）を `.claude/settings.local.json` の `env` に設定済み（gitignore 済み、コミット不可）
    - Claude Code on the web からは現状 `supabase.co` へのアウトバウンドがネットワークポリシーで遮断 → 環境設定に `uqofdnsolmrzxckmtpzv.supabase.co` を許可ホストとして追加するか、ローカルの Claude Code CLI で実行すれば読み取り可能

- [x] **社内メモ行（出力に出ない内部コメント行）** ✅ 2026-06-04
  - 🔒 社内メモ行（_type: 'internal'）を追加。薄いオレンジ背景で視覚区別。
  - プレビュー・PDF・CSV・Excel・メール全出力から除外。オートセーブ・Undo/Redo・プリセット対応。

- [x] **見積テーブルのスプリットビュー対応（列表示トグル）** ✅ 2026-06-04
  - ツールバーに「列 ▼」ドロップダウン追加。カテゴリ/サブコン・利益・備考 の3列を個別に表示/非表示切替。
  - 設定は localStorage（quoteColVis_v1）に保存し次回起動時も維持。

- [x] **メール本文出力に条件・リマーク欄を追加** ✅ 2026-06-04
  - 背景: プレビューのメール本文4メニューで「条件・リマーク」テキストエリアの内容が出力されていなかった
  - 対応: `quote-email.js` の `buildModel()` に `getRemarkText()` を追加。行単位で分割しテーブル内リマーク行と同様に処理。FX自動注記の重複抑制判定にも反映。
