# 見積支援/ — アーカイブ（読み取り専用 / 公開対象外）

このディレクトリは **2026-05-31 に `_archive/` へ退避**しました。
Phase 2b（2026-05-17）以降の **古い単体版スナップショット**であり、現行コードではありません。

## 経緯

- Phase 2b（2026-05-17 完了）で、見積支援アプリのコードは
  [../../フォワーダー支援/](../../フォワーダー支援) に直接マージされました
- その後ポータル側（`js/quote/`）が大きく改修され、本ディレクトリとは**内容が乖離**しています
  （例: ui.js 1,194 行 → ポータル 2,121 行）。もはや同一の「保険コピー」ではありません
- 2026-05-31、編集対象から外して混乱を防ぐため `_archive/見積支援/` へ退避しました
- GitHub Pages の公開対象は `フォワーダー支援/` のみ（[.github/workflows/deploy.yml](../../.github/workflows/deploy.yml)）。
  本ディレクトリは退避前から**一度も公開されていません**

## 現行の見積もり機能はこちら

ポータル [../../フォワーダー支援/index.html](../../フォワーダー支援/index.html) の
「💼 見積 → 📝 見積もり作成」サブタブが唯一の本番実装です。

| 対象 | ポータル側のパス |
|---|---|
| HTML | [../../フォワーダー支援/index.html](../../フォワーダー支援/index.html) の `#tab-quote-make` 配下 |
| CSS | [../../フォワーダー支援/css/quote.css](../../フォワーダー支援/css/quote.css) |
| JS | [../../フォワーダー支援/js/quote/](../../フォワーダー支援/js/quote/) 配下のファイル群 |

**本ディレクトリは編集しないでください。** 編集してもポータル側には反映されません。

## 完全削除について

git 履歴には残るため、退避後さらに 1〜2 ヶ月運用して問題が出なければ、
このディレクトリごと削除して構いません（緊急の参照先が不要になった時点で）。

## 単体起動（参考・退避後パス）

退避に伴い `shared/*.js` への相対参照を `../../フォワーダー支援/shared/` に修正済みです。
ローカル HTTP サーバ越しなら従来どおり起動できます：

```sh
cd ..  # github/202605_コード改修（リポジトリ直下）
python3 -m http.server 18765
# → http://localhost:18765/_archive/見積支援/index.html
```
